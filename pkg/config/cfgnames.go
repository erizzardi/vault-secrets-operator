package config

var (
	// Vault configuration
	vaultUrlConfig = "vault-url"
	tokenConfig    = "vault-token"

	// Controller configuration
	resyncPeriodConfig = "resync-period"
	loopPeriodConfig   = "loop-period"
	logLevelConfig     = "log-level"

	// Kubernetes configuration
	localTestingConfig = "local-testing"
	kubeconfigConfig   = "kubeconfig"
)
